package com.writer.assignment;

import androidx.appcompat.app.AppCompatActivity;

import android.os.Bundle;

public class FirstTime extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_first_time);
//        ImageView imageView = findViewById(R.id.first1);
//        imageView.setBackgroundResource(R.drawable.writeanim);
//        AnimationDrawable myAnimation = (AnimationDrawable) imageView.getBackground();

//Triggers in somewhere else in a thread
//        myAnimation.start();

    }
}